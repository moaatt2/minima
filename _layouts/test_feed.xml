<feed>
    <generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator>

    <link href="{{ site.url }}{{ site.baseurl }}{{ page.url }}" rel="self" type="application/atom+xml"/>

    <link href="{{ site.url }}{{ site.baseurl }}" rel="alternate" type="text/html"/>

    <updated>{{ "now" | date: "%Y-%m-%dT%T%:z" }}</updated>

    <id>{{ site.url }}{{ site.baseurl }}{{ page.url }}</id>

    {%- if site.title -%}
        <title>{{ site.title }}</title>
    {%- endif -%}

    {% if site.description %}
        <subtitle>
            {{ site.description }}
        </subtitle>
    {% endif %}

    {% if site.author %}
        <author>
            {%- if site.author.name -%}
                <name>{{ site.author.name }}</name>
            {%- endif -%}
            {%- if site.author.email -%}
                <email>{{ site.author.email }}</email>
            {%- endif -%}
        </author>
    {% endif %}


    {% if site.feed.max_posts %}
        {% assign post_limit = site.feed.max_posts %}
    {% else %}
        {% assign post_limit = site.posts | size %}
    {% endif %}


    {% for post in site.posts limit:post_limit %}
        <entry>
            <title type="html">{{ post.title }}</title>

            <link href="{{ site.url }}{{ site.baseurl }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title }}" />

            <published>{{ post.date | date: "%Y-%m-%dT%T%:z" }}</published>

            <updated>{{ post.modified_date | default: post.date | date: "%Y-%m-%dT%T%:z" }}</updated>

            <id>{{ site.url }}{{ site.baseurl }}{{ post.url }}</id>

            {% if site.author %}
                <author>
                    {%- if site.author.name -%}
                        <name>{{ site.author.name }}</name>
                    {%- endif -%}
                    {%- if site.author.email -%}
                        <email>{{ site.author.email }}</email>
                    {%- endif -%}
                </author>
            {% endif %}

            {%- for tag in post.tags -%}
                <category term="{{ tag }}" />
            {%- endfor -%}

            {% assign width  = post.main_image_width%}
            {% assign height = post.main_image_height%}
            {%- if post.main_image and post.image_path -%}
                <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ post.image_path }}{{ post.main_image }}"{%if width%} width="{{width}}"{%endif%}{%if height%} height="{{height}}"{%endif%} />
                <media:content xmlns:media="http://search.yahoo.com/mrss/" medium="image" url="{{ post.image_path }}{{ post.main_image }}" />
            {%- endif -%}

        </entry>
    {% endfor %}
</feed>